apiVersion: operator.tinysystems.io/v1alpha1
kind: TinyFlow
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: tinyflow-sample
spec:
  # TODO(user): Add fields here
