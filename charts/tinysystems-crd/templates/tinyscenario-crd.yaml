apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tinyscenarios.operator.tinysystems.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  labels:
  {{- include "tinysystems-crd.labels" . | nindent 4 }}
spec:
  group: operator.tinysystems.io
  names:
    kind: TinyScenario
    listKind: TinyScenarioList
    plural: tinyscenarios
    singular: tinyscenario
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TinyScenario is the Schema for the tinyscenarios API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TinyScenarioSpec defines the desired state of TinyScenario
            properties:
              ports:
                description: Ports contains per-port sample data entries
                items:
                  description: ScenarioPortData stores the sample data for a single
                    port
                  properties:
                    data:
                      description: Data is the JSON-encoded sample payload for this
                        port
                      format: byte
                      type: string
                    port:
                      description: Port is the full port name (e.g., "flowid.module.component-suffix:portname")
                      type: string
                  required:
                  - port
                  type: object
                type: array
            type: object
          status:
            description: TinyScenarioStatus defines the observed state of TinyScenario
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []